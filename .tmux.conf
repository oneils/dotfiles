
# Enable 256-color and true-color (24-bit) support in tmux
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"


# General
set -g set-clipboard on         # Use system clipboard
set -g detach-on-destroy off    # Don't exit from tmux when closing a session
set -g mouse on                 # Enable mouse support
# set -g status-interval 3        # Update the status bar every 3 seconds (default: 15 seconds). Can be usefull for status bar plugins


# Refresh tmux config with r
unbind r
bind r source-file  ~/.tmux.conf 


# Split horizontally in CWD with \
# unbind %
# bind \\ split-window -h -c "#{pane_current_path}"

# Split vertically in CWD with -
# unbind \"
# bind - split-window -v -c "#{pane_current_path}"

# New window in same path
bind c new-window -c "#{pane_current_path}"


# Use vim arrow keys to resize
bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5
bind -r h resize-pane -L 5


# Enable vi mode to allow us to use vim keys to move around in copy mode (Prefix + [ places us in copy mode)
set-window-option -g mode-keys vi

# Start selecting text with "v"
bind-key -T copy-mode-vi 'v' send -X begin-selection 

# Copy text with "y"
bind -T copy-mode-vi 'y' send-keys -X copy-pipe-and-cancel "pbcopy"

# Paste yanked text with "Prefix + P" ("Prefix + p" goes to previous window)
bind P paste-buffer
 
# Don't exit copy mode when dragging with mouse
unbind -T copy-mode-vi MouseDragEnd1Pane


# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set -g renumber-windows on # Automatically renumber windows when one is closed
set-window-option -g pane-base-index 1

set -g status-justify centre
set -g status-right-length 200 #default 10
set -g status-left-length 200 #default 10


# nvim
set-option -g history-limit 25000

# for neovim
set -sg escape-time 10


set -g status-position top



# Switch panes with prefix + h, j, k, l
bind-key h select-pane -L  # Move left
bind-key j select-pane -D  # Move down
bind-key k select-pane -U  # Move up
bind-key l select-pane -R  # Move right


#bind-key -T copy-mode-vi v send-keys -X begin-selection
#bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
#bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

# reload configuration
# bind-key -r r source-file ~/.tmux.conf



## FOr images support


# https://github.com/3rd/image.nvim/?tab=readme-ov-file#tmux
# This is needed by the image.nvim plugin
# set -gq allow-passthrough on

# This is related to the "tmux_show_only_in_active_window = true,' config in
# image.nvim
# set -g visual-activity off


# ==== TPM - Tmux Plugin Manager ====
# List plugins here, e.g.:
# set -g @plugin 'tmux-plugins/tmux-sensible'

set -g @catppuccin_flavour 'mocha'

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'catppuccin/tmux#2.1.3'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-cpu'
set -g @plugin 'tmux-plugins/tmux-battery'

# Initialize TPM (keep this line at the very bottom of .tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

# Override catppuccin's styles (must be after TPM run to take effect)
# set -gF mode-style 'fg=#{@thm_crust},bg=#{@thm_mauve},bold'

# Status bar
# set -gF status-style 'bg=#{@thm_mantle}'
# set -gF status-left '#[fg=#{@thm_green},bold,bg=#{@thm_mantle}]  #S'
# set -gF status-right '#[fg=#{@thm_fg},bg=#{@thm_mantle}]  %Y-%m-%d  %H:%M'
# set -gF window-status-current-format '#[fg=#{@thm_blue},bg=default] • #I:#W'
# set -gF window-status-format '#[fg=#{@thm_fg},bg=default] #I:#W'
# set -gF window-status-last-style 'fg=#{@thm_fg},bg=default'
#
#
set -g @catppuccin_window_status_style "rounded"

# Make the status line pretty and add some modules


run ~/.tmux/plugins/tmux/catppuccin.tmux
# run ~/.tmux/plugins/tmux/cpu.tmux
# run ~/.tmux/plugins/tmux/battery.tmux

set -g status-right-length 100
set -g status-left-length 100
set -g status-left "#{E:@catppuccin_status_session}"
set -g status-right "#{E:@catppuccin_status_application}"
set -agF status-right "#{E:@catppuccin_status_cpu}"
#set -ag status-right "#{E:@catppuccin_status_session}"
set -ag status-right "#{E:@catppuccin_status_uptime}"
set -agF status-right "#{E:@catppuccin_status_battery}"

set -g @catppuccin_window_text "#W"
set -g @catppuccin_window_current_text "#W"

# set -gF window-status-current-format '#[fg=#{@thm_blue},bg=default] • #I:#W'
# set -gF window-status-format '#[fg=#{@thm_fg},bg=default] #I:#W'

run ~/.tmux/plugins/tmux-cpu/cpu.tmux
run ~/.tmux/plugins/tmux-battery/battery.tmux

